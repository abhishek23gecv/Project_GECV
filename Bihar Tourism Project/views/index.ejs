<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Experience Bihar</title>
    <link rel="stylesheet" href="/css/main.css">
    <style>
        /* Hero Section */
        .hero {
            margin-top: 1rem;
            background: url('/images/bihar.jpg') no-repeat center center/cover;
            height: 50vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
        }

        @media (min-width: 768px) {
            .hero {
                height: 600px;
            }
        }

        .hero-content {
            background-color: rgba(195, 114, 89, 0.4);
            -webkit-backdrop-filter: blur(5px);
            backdrop-filter: blur(5px);
            padding: 20px;
            border-radius: 5px;
        }

        .hero h1 {
            font-size: 2rem;
        }

        @media (min-width: 768px) {
            .hero h1 {
                font-size: 3rem;
            }
        }

        .hero p {
            font-size: 1rem;
            margin-bottom: 1rem;
        }

        @media (min-width: 768px) {
            .hero p {
                font-size: 1.5rem;
            }
        }

        .hero .btn {
            padding: 10px 20px;
            background: #f39c12;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-size: 0.9rem;
        }

        .hero .btn:hover {
            background: #d35400;
        }

        /* Slider Section */
        .slider-wrapper {
            position: relative;
            max-width: 100%;
            overflow: hidden;
            height: 300px;
        }

        @media (min-width: 768px) {
            .slider-wrapper {
                height: 900px;
            }
        }

        .slider {
            display: flex;
            transition: transform 0.5s ease-in-out;
        }

        .slide {
            min-width: 100%;
            box-sizing: border-box;
            position: relative;
        }

        .slide img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .caption {
            position: absolute;
            bottom: 10%;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0, 0, 0, 0.7);
            color: #fff;
            padding: 10px;
            border-radius: 5px;
            font-size: 1rem;
            text-align: center;
        }

        .slider-controls {
            position: absolute;
            top: 50%;
            width: 100%;
            display: flex;
            justify-content: space-between;
            transform: translateY(-50%);
        }

        .prev,
        .next {
            cursor: pointer;
            font-size: 24px;
            color: white;
            padding: 10px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 50%;
        }

        .prev:hover,
        .next:hover {
            background: rgba(0, 0, 0, 0.8);
        }

        @media (max-width: 768px) {

            .prev,
            .next {
                font-size: 20px;
                padding: 8px;
            }

            .slider-wrapper {
                height: 400px;
            }
        }

        /* Video Section */
        .video {
            text-align: center;
            margin: 1rem 0;
        }

        .video video {
            width: 100%;
            border-radius: 10px;
        }

        /* Cards Section */
        .cards {
            padding: 2rem 1rem;
            text-align: center;
        }

        .card-container {
            display: grid;
            gap: 20px;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            align-items: center;
        }
        .card-container a {
            text-decoration: none;
        }
        .card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        .card img {
            width: 100%;
            height: 200px;
            border-bottom: 2px solid #f39c12;
        }

        .card h3 {
            font-size: 1.2rem;
            margin: 1rem 0;
            color: #333;
        }

        .card p {
            padding: 0 1rem 1rem;
            color: #666;
            font-size: 0.9rem;
        }

        /* General */
        h2 {
            text-align: center;
            margin-bottom: 1rem;
        }

        @media (min-width: 768px) {
            h2 {
                font-size: 2rem;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/navbar') %>

        <header class="hero">
            <div class="hero-content">
                <h1>Welcome to Bihar</h1>
                <p>Discover the land of history, culture, and adventure.</p>
                <a href="#explore" class="btn">Explore Now</a>
            </div>
        </header>

        <section class="video">
            <h2>Experience Bihar</h2>
            <video controls autoplay loop muted>
                <source src="/images/video.mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        </section>

        <section id="explore" class="explore">
            <h2>Top Attractions</h2>
            <div class="slider-wrapper">
                <div class="slider">
                    <div class="slide">
                        <img src="/images/mahabodhi mandir.jpg" alt="Mahabodhi Temple">
                        <div class="caption">Mahabodhi Temple</div>
                    </div>
                    <div class="slide">
                        <img src="/images/bodh stupa.jpg" alt="Bodh Stupa">
                        <div class="caption">Bodh Stupa</div>
                    </div>
                </div>
                <div class="slider-controls">
                    <span class="prev" onclick="plusSlides(-1)">&#10094;</span>
                    <span class="next" onclick="plusSlides(1)">&#10095;</span>
                </div>
            </div>
        </section>

        <section class="cards">
            <h2>Explore More</h2>
            <div class="card-container">
                <a href="/attractions">
                    <div class="card">
                        <img src="/images/attractions/biharTP.jpg" alt="attraction">
                        <h3>Attractions of Bihar</h3>
                        <p>Experience the vibrant places for adventures.</p>
                    </div>
                </a>
                <a href="/adventure">
                    <div class="card">
                        <img src="/images/adventure/tutla.jpg" alt="Adventure">
                        <h3>Adventure Hub</h3>
                        <p>Unleash your adventurous spirit in Bihar.</p>
                    </div>
                </a>
                <a href="/festivals">
                    <div class="card">
                        <img src="images/attractions/Chhath-Puja.png.webp" alt="Festivals">
                        <h3>Festivals of Bihar</h3>
                        <p>Experience the vibrant culture through unique festivals.</p>
                    </div>
                </a>
                <a href="/cuisine">
                    <div class="card">
                        <img src="/images/cuisine/food.jpg" alt="Cuisine">
                        <h3>Bihar Cuisine</h3>
                        <p>Delight in the taste of authentic Bihari dishes.</p>
                    </div>
                </a>

                <a href="/crafts">
                    <div class="card">
                        <img src="/images/craft.jpg" alt="crafts">
                        <h3>crafts of Bihar</h3>
                        <p>Experience the vibrant culture through unique crafts.</p>
                    </div>
                </a>
            </div>
        </section>

        <%- include('partials/footer') %>

            <script>
                let slideIndex = 0;
                const slides = document.querySelectorAll(".slide");

                function showSlides() {
                    slides.forEach(slide => slide.style.display = "none");
                    slideIndex++;
                    if (slideIndex > slides.length) {
                        slideIndex = 1;
                    }
                    slides[slideIndex - 1].style.display = "block";
                    setTimeout(showSlides, 5000);
                }

                function plusSlides(n) {
                    slideIndex += n - 1;
                    if (slideIndex < 0) {
                        slideIndex = slides.length - 1;
                    } else if (slideIndex >= slides.length) {
                        slideIndex = 0;
                    }
                    slides.forEach(slide => slide.style.display = "none");
                    slides[slideIndex].style.display = "block";
                }

                showSlides();
            </script>
</body>

</html>